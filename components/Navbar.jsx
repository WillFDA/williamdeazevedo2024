import Logo from "./Logo";

const Navbar = () => {
  return (
    <nav className="max-container flex justify-center items-center py-16">
      <Logo delay="0.2" />
    </nav>
  );
};

export default Navbar;
